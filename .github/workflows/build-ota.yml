name: Build ESP32 OTA firmware (fast)

on:
  workflow_dispatch:
    inputs:
      code_b64:
        description: "Contenido de user_app.cpp en Base64"
        required: true
      ssid:
        description: "WiFi SSID"
        required: true
      pass:
        description: "WiFi password"
        required: true

# Evita colas si disparas muchas veces seguidas (mejora “tiempo percibido”)
concurrency:
  group: esp32-ota-main
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ARDUINO_CLI_VERSION: "0.35.3"
      ESP32_CORE_VERSION: "3.3.6"
      # build-cache REAL (persistible y cacheable)
      BUILD_CACHE_DIR: "~/.cache/arduino-build-cache"

    steps:
      # Checkout más rápido (solo lo necesario)
      - name: Checkout repo (sparse)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            sketch
            .github/workflows/build-ota.yml
          sparse-checkout-cone-mode: true

      # Cache MUCHO más chico: solo ESP32 (no todo packages)
      - name: Cache ESP32 core/tools (smaller)
        uses: actions/cache@v4
        with:
          path: |
            ~/.arduino15/packages/esp32
            ~/.arduino15/staging
          key: ${{ runner.os }}-esp32-${{ env.ESP32_CORE_VERSION }}-cli-${{ env.ARDUINO_CLI_VERSION }}-v2

      # Cache REAL del build-cache
      - name: Cache Arduino build-cache (real)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/arduino-build-cache
          key: ${{ runner.os }}-buildcache-esp32-${{ env.ESP32_CORE_VERSION }}-v2

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v2
        with:
          version: ${{ env.ARDUINO_CLI_VERSION }}

      - name: Setup ESP32 core (only if missing)
        shell: bash
        run: |
          set -e
          arduino-cli config init || true
          arduino-cli config set board_manager.additional_urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

          if arduino-cli core list | grep -qE "esp32:esp32[[:space:]]+${ESP32_CORE_VERSION}"; then
            echo "ESP32 core ${ESP32_CORE_VERSION} OK ✅"
          else
            echo "Installing core..."
            arduino-cli core update-index
            arduino-cli core install esp32:esp32@${ESP32_CORE_VERSION}
          fi

      - name: Inyectar WiFi y user_app.cpp
        shell: bash
        env:
          WIFI_SSID: ${{ github.event.inputs.ssid }}
          WIFI_PASS: ${{ github.event.inputs.pass }}
          CODE_B64: ${{ github.event.inputs.code_b64 }}
        run: |
          set -e
          mkdir -p sketch
          cat > sketch/config_wifi.h <<EOF
          #pragma once
          #define WIFI_SSID "${WIFI_SSID}"
          #define WIFI_PASS "${WIFI_PASS}"
          EOF
          echo "$CODE_B64" | base64 -d > sketch/user_app.cpp

      - name: Normalizar user_app.cpp (setup/loop -> UserSetup/UserLoop)
        shell: bash
        run: |
          set -e
          if grep -qE '^[[:space:]]*void[[:space:]]+setup[[:space:]]*\(' sketch/user_app.cpp; then
            sed -i -E 's/^[[:space:]]*void[[:space:]]+setup[[:space:]]*\(/void UserSetup(/' sketch/user_app.cpp
          fi
          if grep -qE '^[[:space:]]*void[[:space:]]+loop[[:space:]]*\(' sketch/user_app.cpp; then
            sed -i -E 's/^[[:space:]]*void[[:space:]]+loop[[:space:]]*\(/void UserLoop(/' sketch/user_app.cpp
          fi
          grep -qE 'void[[:space:]]+UserSetup[[:space:]]*\(' sketch/user_app.cpp
          grep -qE 'void[[:space:]]+UserLoop[[:space:]]*\(' sketch/user_app.cpp

      - name: Compilar sketch (FAST + jobs)
        shell: bash
        run: |
          set -e
          mkdir -p "$HOME/.cache/arduino-build-cache"
          BUILD_PATH="${RUNNER_TEMP}/arduino-build"
          mkdir -p "${BUILD_PATH}"

          JOBS="$(nproc || echo 2)"
          echo "Using jobs=${JOBS}"

          arduino-cli compile --fqbn esp32:esp32:esp32da sketch \
            --output-dir build \
            --build-path "${BUILD_PATH}" \
            --build-cache-path "$HOME/.cache/arduino-build-cache" \
            --jobs "${JOBS}"

          ls -la build

      - name: Preparar firmware
        shell: bash
        run: |
          set -e
          BIN_PATH=$(find build -maxdepth 1 -type f -name "*sketch.ino.bin" | head -n 1)
          test -n "$BIN_PATH"
          mkdir -p out
          cp "$BIN_PATH" out/firmware.bin
          ls -la out/firmware.bin

      - name: Subir artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: out/firmware.bin
