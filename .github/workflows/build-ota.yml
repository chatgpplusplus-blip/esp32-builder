name: Build ESP32 OTA firmware

on:
  workflow_dispatch:
    inputs:
      code_b64:
        description: "Contenido de user_app.cpp en Base64"
        required: true
      ssid:
        description: "WiFi SSID"
        required: true
      pass:
        description: "WiFi password"
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v2
        with:
          # Usamos una versión fija para evitar el error "unable to get latest version"
          version: 0.35.3

      - name: Setup ESP32 core
        run: |
          arduino-cli config init
          arduino-cli config set board_manager.additional_urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core update-index
          arduino-cli core install esp32:esp32

      # (Opcional pero útil) Ver boards disponibles si algún día cambias el FQBN
      - name: Listar boards ESP32 disponibles (debug)
        run: |
          arduino-cli board listall esp32:esp32 | head -n 80

      - name: Descargar PubSubClient y copiarlo a sketch
        run: |
          mkdir -p lib
          git clone https://github.com/knolleary/pubsubclient.git lib/PubSubClient
          mkdir -p sketch
          cp lib/PubSubClient/src/PubSubClient.* sketch/
          echo "Contenido de sketch tras copiar PubSubClient:"
          ls -R sketch

      - name: Inyectar WiFi y user_app.cpp
        env:
          WIFI_SSID: ${{ github.event.inputs.ssid }}
          WIFI_PASS: ${{ github.event.inputs.pass }}
          CODE_B64: ${{ github.event.inputs.code_b64 }}
        run: |
          mkdir -p sketch

          # Crear config_wifi.h con las credenciales WiFi
          echo '#pragma once' > sketch/config_wifi.h
          echo '#define WIFI_SSID "'$WIFI_SSID'"' >> sketch/config_wifi.h
          echo '#define WIFI_PASS "'$WIFI_PASS'"' >> sketch/config_wifi.h

          # Crear user_app.cpp desde base64
          echo "$CODE_B64" | base64 -d > sketch/user_app.cpp

          echo "Archivos en sketch después de inyectar user_app:"
          ls -R sketch

      - name: Compilar sketch
        run: |
          # IMPORTANTE:
          # Si tu FQBN cambia, ajústalo aquí.
          # Por ahora dejamos tu mismo FQBN para no romperte nada.
          arduino-cli compile --fqbn esp32:esp32:esp32da sketch --output-dir build
          echo "Contenido de build/"
          ls -la build

      - name: Preparar firmware como archivo de salida
        run: |
          # Preferimos el bin principal de la app (normalmente *sketch.ino.bin*)
          BIN_PATH=$(find build -maxdepth 1 -type f -name "*sketch.ino.bin" | head -n 1)

          # Fallback: si no existe con ese nombre, agarramos el primer .bin que NO sea bootloader/partitions
          if [ -z "$BIN_PATH" ]; then
            BIN_PATH=$(find build -maxdepth 1 -type f -name "*.bin" \
              ! -name "*bootloader*" ! -name "*partitions*" | head -n 1)
          fi

          echo "BIN_PATH=$BIN_PATH"
          if [ -z "$BIN_PATH" ]; then
            echo "No se encontró firmware .bin en build/"
            echo "Listado de build:"
            ls -la build
            exit 1
          fi

          mkdir -p out
          cp "$BIN_PATH" out/firmware.bin
          echo "Firmware final:"
          ls -la out/firmware.bin

      - name: Subir firmware como artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: out/firmware.bin
